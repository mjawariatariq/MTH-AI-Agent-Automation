import google.generativeai as genai
import os
from dotenv import load_dotenv

load_dotenv()
genai.configure(api_key=os.getenv("GOOGLE_API_KEY"))
model = genai.GenerativeModel('gemini-2.5-flash')
"""
TASK 1: Basic Data Extraction
"""

import google.generativeai as genai
import os
import json
from dotenv import load_dotenv

load_dotenv()
genai.configure(api_key=os.getenv("GOOGLE_API_KEY"))
model = genai.GenerativeModel('gemini-2.5-flash')

# messy_text = "Hey! My name is Ahmed Khan and I live in Lahore. You can contact me at 0300-1234567. Thanks!"

# YOUR PROMPT HERE
prompt = f"""
Extract name, phone, and city from this text:

Return ONLY JSON with keys: "name", "phone", "city"
"""

print("\n" + "="*50)
print(" Try with your own text:")
text = input("Enter messy text: ")
if text:
    response = model.generate_content(prompt)
    print("\n Result:")
    print(response.text)
